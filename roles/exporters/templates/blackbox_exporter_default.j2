# Configuration for blackbox_exporter
# This file is managed by Ansible

# Additional command line options
# ICMP probes require root privileges
ARGS="--config.file=/etc/prometheus/blackbox.yml --web.listen-address=127.0.0.1:{{ blackbox_exporter_port | default('9115') }}"
