---
- name: add user "{{ user }}"
  become_method: su
  become: true
  user:
    name: "{{ user }}"
    password: "{{ password }}"
    groups: sudo
    shell: /bin/zsh
    state: present
  when: password is defined
